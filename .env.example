DATABASE_URL="file:./prisma/dev.db"

# Secret used to sign session cookies (staff + participant login).
# Generate your own with: openssl rand -base64 32
SESSION_SECRET="change-me-to-a-long-random-string"
